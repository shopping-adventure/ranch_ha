% -*- mode: erlang -*-
{auto_compile, false}.

{logdir, all_nodes, "@TOPDIR@/logs"}.
{logdir, master, "@TOPDIR@/logs"}.

{alias, test, "@TEST_DIR@"}.

{node, a, 'nodeA@aquila.lan'}.
{node, b, 'nodeB@aquila.lan'}.
{node, c, 'nodeC@aquila.lan'}.
{node, d, 'nodeD@aquila.lan'}.

{init, [a,b,c,d], {node_start, [
				{monitor_master, true},
				{erl_flags, "-pa @PATHS@"},
				{startup_functions, [{application, ensure_all_started, [ranch_ha]}]}
			       ]}}.

{suites, [a,b,c,d], test, all}.
